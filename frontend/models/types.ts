export type playerType = {
  name: string;
  counts: number;
  is_flag: boolean;
  LastIn: string;
};

export type playerListType = playerType[];

export type Info = {
  title: string;
  description: string;
};
